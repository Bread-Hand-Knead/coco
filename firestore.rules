rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 開發期安全規則：允許所有已登入使用者讀取與寫入資料
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
